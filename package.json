{
  "scripts": {
    "build": "NODE_ENV=production rollup -c rollup.config.js & cp index.html dist/ & cp index.css dist/",
    "start": "rollup -w -c rollup.config.js"
  },
  "devDependencies": {
    "babel-preset-es-uxtemple": "^1.0.0",
    "babel-plugin-external-helpers": "^6.5.0",
    "rollup": "^0.34.10",
    "rollup-plugin-babel": "^2.6.1",
    "rollup-plugin-browserify-transform": "^0.1.0"
  }
}
